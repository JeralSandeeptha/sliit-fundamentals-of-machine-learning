# Like a list but it includes different types of data types
# Group of diffrent data types

jeral = ("Jeral", 25, "Ragama");

name = jeral[0];
age = jeral[1];

# We can't remove elements from a tuple directly. If we want, then we need to convert tuple into a list. then we can remove

